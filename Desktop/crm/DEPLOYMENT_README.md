# 🚀 دليل النشر السريع - CRM

## 📥 التثبيت الأولي (مرة واحدة فقط)

### 1️⃣ تثبيت المتطلبات على السيرفر:
- **Node.js 18+**: https://nodejs.org/
- **Git** (اختياري): https://git-scm.com/

### 2️⃣ تشغيل سكريبت النشر التلقائي:
```powershell
# افتح PowerShell كمسؤول وانتقل لمجلد المشروع
cd C:\inetpub\crm

# تشغيل سكريبت النشر
.\scripts\deploy.ps1
```

**هذا السكريبت سيقوم بـ:**
- ✅ التحقق من المتطلبات
- ✅ تثبيت PM2
- ✅ تثبيت المكتبات
- ✅ بناء التطبيق
- ✅ بدء التطبيق
- ✅ تفعيل التشغيل التلقائي
- ✅ فتح المنفذ في Firewall

---

## 🔄 التحديث (بعد تعديل الكود)

### الطريقة الأولى: تحديث تلقائي مع Git
```powershell
cd C:\inetpub\crm
.\scripts\update.ps1 -WithGit -NoDowntime
```

### الطريقة الثانية: تحديث يدوي
1. انسخ الملفات المحدثة إلى السيرفر
2. شغّل السكريبت:
```powershell
cd C:\inetpub\crm
.\scripts\update.ps1 -NoDowntime
```

---

## 🎮 أوامر PM2 المهمة

```powershell
# عرض حالة التطبيق
pm2 status

# عرض السجلات مباشرة
pm2 logs crm-app

# عرض آخر 50 سطر من السجلات
pm2 logs crm-app --lines 50

# إعادة تشغيل التطبيق
pm2 restart crm-app

# إعادة تشغيل بدون انقطاع
pm2 reload crm-app

# إيقاف التطبيق
pm2 stop crm-app

# بدء التطبيق
pm2 start crm-app

# مراقبة الأداء
pm2 monit

# معلومات مفصلة
pm2 info crm-app

# مسح السجلات
pm2 flush
```

---

## 🌐 الوصول إلى التطبيق

بعد النشر، يمكن الوصول إلى التطبيق عبر:
- **محلياً**: http://localhost:3000
- **من الشبكة**: http://[IP-السيرفر]:3000

---

## ⚙️ ملف البيئة `.env.local`

تأكد من وجود هذا الملف في مجلد المشروع:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# App
NEXT_PUBLIC_APP_URL=http://your-server-ip:3000
NODE_ENV=production
```

---

## 🔍 استكشاف الأخطاء

### التطبيق لا يعمل؟
```powershell
# 1. تحقق من الحالة
pm2 status

# 2. شاهد السجلات
pm2 logs crm-app

# 3. أعد بناء التطبيق
npm run build

# 4. أعد التشغيل
pm2 restart crm-app
```

### لا يعمل بعد إعادة تشغيل السيرفر؟
```powershell
# تأكد من التشغيل التلقائي
pm2 save
pm2 startup
pm2-startup install
```

### خطأ في الاتصال بـ Supabase؟
- تحقق من ملف `.env.local`
- تأكد من صحة المفاتيح
- تحقق من اتصال الإنترنت

---

## 📊 المراقبة

### مراقبة الأداء:
```powershell
pm2 monit
```

### مشاهدة استهلاك الموارد:
```powershell
pm2 list
```

### إحصائيات مفصلة:
```powershell
pm2 info crm-app
```

---

## 🔒 الأمان

### إضافة HTTPS (مستحسن):
استخدم **IIS** أو **Nginx** كـ Reverse Proxy مع شهادة SSL

### الملفات المهمة:
- `web.config` - إعداد IIS
- `ecosystem.config.js` - إعداد PM2
- `.env.local` - **لا ترفعه على Git!**

---

## 📝 ملاحظات

✅ **التحديثات**: استخدم `-NoDowntime` لتحديث بدون انقطاع
✅ **السجلات**: موجودة في `logs/out.log` و `logs/err.log`
✅ **الأداء**: PM2 يُعيد التشغيل تلقائياً عند الأخطاء
✅ **الذاكرة**: يُعاد التشغيل تلقائياً عند تجاوز 1GB (قابل للتعديل)

---

## 🆘 الدعم

للحصول على دعم مفصل، راجع: **DEPLOYMENT_GUIDE.md**

---

**تم! 🎉** التطبيق الآن جاهز للاستضافة الاحترافية على سيرفر ويندوز.

