# 🎯 ابدأ من هنا - نشر CRM على سيرفر ويندوز

## خطوة واحدة فقط! 🚀

### 1️⃣ افتح PowerShell كمسؤول
```powershell
# انتقل لمجلد المشروع
cd C:\inetpub\crm

# شغّل سكريبت النشر التلقائي
.\scripts\deploy.ps1
```

**سيقوم السكريبت بكل شيء تلقائياً!** ☕

---

## ✅ تم! التطبيق يعمل الآن

افتح المتصفح: **http://localhost:3000**

---

## 🔄 عند التحديث (بعد تعديل الكود)

### خيار 1: مع Git
```powershell
.\scripts\update.ps1 -WithGit -NoDowntime
```

### خيار 2: بدون Git (رفع ملفات يدوياً)
```powershell
.\scripts\update.ps1 -NoDowntime
```

---

## 📋 أوامر مختصرة مفيدة

```powershell
pm2 status          # حالة التطبيق
pm2 logs crm-app    # السجلات
pm2 restart crm-app # إعادة تشغيل
pm2 monit          # مراقبة الأداء
```

---

## ⚠️ مهم جداً!

**قبل النشر الأول:**
1. ✅ أنشئ ملف `.env.local` في مجلد المشروع
2. ✅ أضف معلومات Supabase فيه:
```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi....
NEXT_PUBLIC_APP_URL=http://server-ip:3000
NODE_ENV=production
```

---

## 🎁 ميزات إضافية

✨ **يعمل تلقائياً عند تشغيل السيرفر**
✨ **يُعاد تشغيله تلقائياً عند الأخطاء**
✨ **تحديثات بدون توقف باستخدام `-NoDowntime`**
✨ **سجلات كاملة في مجلد `logs/`**

---

## 🆘 مشكلة؟

**التطبيق لا يعمل:**
```powershell
pm2 logs crm-app    # شاهد الأخطاء
npm run build       # أعد البناء
pm2 restart crm-app # أعد التشغيل
```

**للتفاصيل الكاملة**: اقرأ `DEPLOYMENT_README.md` أو `DEPLOYMENT_GUIDE.md`

---

## 🏁 انتهى!

**كل شيء جاهز!** استمتع بتطبيق CRM الاحترافي على السيرفر 🎉

